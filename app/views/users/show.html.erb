<!-- TODO make this only visible to people who share a trip with @user -->
<h3><%= @user.email %> is interested in the following destinations:</h3>

<!-- TODO this could be a shared partial -->
<ul>
  <% @user.destinations.each do |destination| %>
  <li><%= destination %></li>
  <% end %>
</ul>

<h3><%= @user.email %> is busy on the following dates:</h3>
<div id='user_calendar'></div>
<div id='user_calendar_url' value="<%= user_path(@user)%>.json"></div>
