<h1><%= @trip.name %></h1>

Trip members:
<ul>
  <% @trip.users.each do |user| %>
    <% unless user.invitation_token %>
      <li><%= user.email %></li>
    <% end %>
  <% end %>
</ul>

<%= link_to "Invite a friend!", trip_invite_path(@trip) %>